circle(x, y, radius)
rectangle(x0, y0, x1, y1)
line(x0, y0, x1, y1)
point(x, y)
arc(x, y, radius)

CURSOR cursor1(0,0)
hide()
color(int, int, int)
pensize(size)
up()
down()
move(x,y)
rotation(degrees)

FOR 0 TO 10 STEP 1 { }

BLOC drawSquareAndCircle { }

WHILE expression { }

DO { } WHILE expression

FOR_STATEMENT
    ::= KEYWORD("FOR") 
		EXPRESSION KEYWORD("STEP") ASSIGNMENT_EXPR
        BLOCK
    ;
	
WHILE_STATEMENT
    ::= KEYWORD("WHILE")
        DELIMITER("(") EXPRESSION DELIMITER(")")
        BLOCK
    ;